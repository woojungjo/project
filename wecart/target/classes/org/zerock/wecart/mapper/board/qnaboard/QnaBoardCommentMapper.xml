<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.wecart.mapper.board.qnaboard.QnaBoardCommentMapper">
	
  <insert id="insertComment">
  
    <selectKey keyProperty="comment_no" order="AFTER" resultType="int">
      SELECT ISEQ$$_109032.currval FROM dual
    </selectKey>
    INSERT INTO qna_board_comment(post_no, member_id, secret_yn, content,like_cnt, high_comment_no, comment_lv)
    VALUES(#{post_no}, #{member_id}, #{secret_yn}, #{content}, #{like_cnt}, #{high_comment_no}, #{comment_lv})


  </insert>
	



</mapper>